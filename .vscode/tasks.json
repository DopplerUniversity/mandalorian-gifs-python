{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 for spec
    "version": "2.0.0",
    "tasks": [
        {
            "label": "doppler-env-create",
            "type": "shell",
            "command": "doppler secrets download --no-file --format env > ${workspaceFolder}/.env"
        },
        {
            "label": "doppler-env-remove",
            "type": "shell",
            "command": "rm ${workspaceFolder}/.env"
        }
    ]
}